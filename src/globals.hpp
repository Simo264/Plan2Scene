#pragma once 

#include "types.hpp"

extern i32 window_width;
extern i32 window_height;

extern f64 unit_scale;

extern f64 snap_eps;

extern i32 cluster_num_samples;
extern f64 cluster_eps;

extern f32 ceil_height_meters;
extern f32 door_frac_top;
extern f32 window_frac_bottom;
extern f32 window_frac_top;

extern f32 floor_texture_scaling;
extern f32 wall_texture_scaling;
